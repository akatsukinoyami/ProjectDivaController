73 (I),		△		每一行會對應到一個設定
74 (J),		□		只有第一個單字會被解析為設定(以空白分割)，因此後面全是註解
75 (K),		×		前12個設定是按鍵配置
76 (L),		◯		請使用十進位數字的Virtual-Key Codes，具體數值請參考https://learn.microsoft.com/en-us/windows/win32/inputdev/virtual-key-codes
W (VK code:87),	🡅		如果是字母按鍵，你也可以使用單一大寫字母而不是Virtual-Key Codes，就像上面的J和K一樣。
A (VK code:65),	🡄		Each line corresponds to a setting. Only the first word is interpreted as a setting (separated by spaces), therefore the rest are comments.
S (VK code:83),	🡇		The first 12 settings are for key configuration. Please use decimal Virtual-Key Codes. You can refer to https://learn.microsoft.com/en-us/windows/win32/inputdev/virtual-key-codes
D (VK code:68),	🡆		If it's a letter key, you can also use a single uppercase letter instead of Virtual-Key Codes, like WASD above.
Q (VK code:81),	[↼ 		這裡是滑鍵設定
E (VK code:69),	    ⇀]		Slide button settings.
U (VK code:85),		[↼ 
O (VK code:79),		    ⇀]		
1.0	slide_require_multiplier	此設定為滑動的閥值，值越大則需要越大的速度才會判定為滑動
1	output_received_message		是否輸出從平板收到的信息
1	output_keyboard_operation	是否輸出所進行的按鍵操作與按鍵狀態